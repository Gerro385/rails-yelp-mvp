<h1>Make a new Review for <%= @restaurant.name %></h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.label :content %>
  <%= f.input :content %>
  <%= f.label :rating %>
  <%= f.input :rating %>
  <%= f.button :submit %>
<% end %>